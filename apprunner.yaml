version: 1.0
runtime: python3
build:
  commands:
    build:
      - python3 -m venv venv
      - source venv/bin/activate
      - pip install -r requirements.txt

run:
  runtime-version: 3.8.16
  command: sh startup.sh
  network:
    port: 8000