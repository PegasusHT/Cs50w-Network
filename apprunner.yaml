version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install -r requirements.txt
  runtime-version: 3.8.16

run:
  command: sh startup.sh
  network:
    port: 8000